Metadata-Version: 2.4
Name: terminal-game-hub
Version: 1.0.0
Summary: Terminal game hub for Linux — play board games against OpenClaw
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: python-chess>=1.999
Requires-Dist: PyYAML>=6.0

# Terminal Game Hub

A lightweight terminal game hub for Linux (including Raspberry Pi 3B) where **you** play board games against **OpenClaw**.

The hub owns all game state, move validation, logging, saves, and replays. OpenClaw connects over the **Gateway HTTP API** (`/v1/chat/completions` + Bearer token — stdlib only, no WebSocket deps).

## Supported games

Only turn-based board games — no real-time or physics simulations:

| Game | OpenClaw role | Move authority |
|------|---------------|----------------|
| Tic-Tac-Toe | Strategic opponent | Hub Move Gate |
| Checkers | Strategic opponent | Hub Move Gate |
| Chess Mode A (default) | Strategic opponent | Hub Move Gate |
| Chess Mode B (optional) | Commentary/analysis | Stockfish moves, hub validates |

**Key rule:** OpenClaw may think strategically. The local game engine enforces reality.

## Quick start

```bash
pip install -e .
game-hub init-config
# Edit ~/.game-hub/config.yaml — set openclaw.base_url and openclaw.token
game-hub test-connection   # must pass before playing
game-hub menu
```

## If OpenClaw is unavailable

The hub does **not** switch to a local AI. You get:

- A clear connection error
- Retry, save-and-exit, or quit
- Optional human-vs-human mode from the menu

## Commands

```bash
game-hub test-connection
game-hub play tictactoe --opponent openclaw
game-hub play chess --chess-mode openclaw      # Mode A (default)
game-hub play chess --chess-mode stockfish     # Mode B
game-hub resume ~/.game-hub/saves/<id>.json
game-hub replay ~/.game-hub/saves/<id>.json
```

## Turn logging

Every turn records: `game_id`, `game_type`, `turn_number`, `actor`, `board_state_before`, `legal_moves`, `selected_move`, `accepted`, `rejection_reason`, `board_state_after`, `timestamp`.

## Docs

- [docs/OPENCLAW_AGENT_GUIDE.md](docs/OPENCLAW_AGENT_GUIDE.md)
- [docs/OPENCLAW_AGENT_PROMPT.md](docs/OPENCLAW_AGENT_PROMPT.md)
